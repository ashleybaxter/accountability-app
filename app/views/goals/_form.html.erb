<%= form_for @goal do |f| %>
<fieldset>
	<%= f.text_field :title, :placeholder => "Add a new goal, then hit enter" %>
</fieldset>	
<%= link_to "Save all of today's goals", new_goal_path, :class => "finished", remote: true %>
<% end %>